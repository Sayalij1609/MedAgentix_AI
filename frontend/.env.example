# MedAgentix Frontend Environment Variables
# If connecting directly to your deployed backend API (e.g. on Render, Railway, AWS):
# VITE_API_BASE_URL=https://your-backend-api.onrender.com/api/v1
# If left empty, defaults to '/api/v1' (for local Vite dev proxy or same-domain proxy)
VITE_API_BASE_URL=
